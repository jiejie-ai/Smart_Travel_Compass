# 改错日志

# RAGFlow 知识库检索始终返回空结果

**日期**：2026-06-27  
**项目**：AI 旅游搭子（ai-tourism）  
**影响范围**：Agent 无法读取本地知识库，所有回答完全依赖联网搜索

---

### 问题现象

用户提问后，Agent 会调用 RAGFlow 知识库检索工具，但**每次都返回"未找到相关信息"**。观察日志：

```
RAGFlow 多查询扩展检索：原始查询='北京小众景点'，扩展为 5 个子查询
所有扩展查询均未返回结果，兜底使用原始查询
```

6 次 HTTP 请求全部返回 200，但 chunks 始终为空数组。用户特意在知识库中上传了测试文档（"彩虹胡同"小众景点），Agent 的回答中完全没有出现该信息——说明**知识库检索环节形同虚设**。

---

### 排查过程

#### 第一步：确认 Agent 是否真的调用了工具

查看服务端日志，发现 `RAGFlow 多查询扩展检索` 确实被触发，工具被正确调用，请求也发送到了 RAGFlow 服务器（`192.168.138.137:81`），排除了"工具未调用"的假设。

#### 第二步：确认知识库是否有数据

在浏览器中登录 RAGFlow 管理界面（`192.168.138.137:81`），确认知识库 `AI_tourism_master` 中已上传测试文档且解析成功。排除了"知识库为空"的假设。

#### 第三步：直接调用 RAGFlow API

绕过项目代码，用 Python 直接向 RAGFlow API 发请求：

```python
# 第一次尝试 — 使用代码中的参数名
body = {"question": "彩虹胡同", "kb_ids": ["21423b..."], "page": 1, "size": 10}
# 返回 {"code": 102, "message": "`dataset_ids` is required."}
```

关键发现：**API 要求的参数是 `dataset_ids`，代码传的是 `kb_ids`**。

#### 第四步：验证修复

```python
# 改为正确的参数名
body = {"question": "彩虹胡同", "dataset_ids": ["21423b..."], "page": 1, "size": 10}
# 返回 {"code": 0, "chunks": [{"content": "...彩虹胡同...门票88元..."}]}
```

数据正常返回。

---

### 根因

`src/tools/ragflow_search.py` 第 47 行，请求体中 JSON key 写错：

```python
# 错误
json={"question": query, "kb_ids": [settings.ragflow_kb_id], ...}

# 正确
json={"question": query, "dataset_ids": [settings.ragflow_kb_id], ...}
```

RAGFlow API 收到无效参数后返回 `{"code": 102, "message": "dataset_ids is required"}`，但 HTTP 状态码是 200，代码只做了 `raise_for_status()` 然后读 `data.chunks`，拿到空数组就返回"未找到"，**真正的错误信息被静默丢弃了**。

---

### 修复

**文件**：`src/tools/ragflow_search.py`  
**改动**：1 行，`kb_ids` → `dataset_ids`

---

### 经验总结

| 要点                 | 说明                                                                                                            |
| ------------------ | ------------------------------------------------------------------------------------------------------------- |
| **不要只信任 HTTP 状态码** | 很多 API（包括 RAGFlow）在业务错误时仍返回 200，真正的错误码在 response body 里。应该检查 `code` 字段而非仅靠 HTTP status。                       |
| **善用"烟雾测试"**       | 在知识库里放一条只有你知道的假数据，能快速验证端到端链路是否打通。这个方法比看日志高效得多。                                                                |
| **最小化复现环境**        | 用 `curl` 或独立 Python 脚本直接调 API，绕过 Agent/Graph 等复杂链路，排除了 LangChain、提示词、Query 扩展等干扰项。                            |
| **编码问题会误导判断**      | Windows PowerShell 的 GBK 编码导致 curl 发出的中文变成乱码，一度以为是终端问题。用 Python 脚本发请求避免了编码陷阱。                                 |
| **API 文档 vs 代码惯例** | 变量名 `kb_ids` 逻辑上很合理（knowledge base ids），但 RAGFlow 实际 API 用的是 `dataset_ids`。集成第三方服务时不能靠猜测参数名，应以 API 文档或实际响应为准。 |

---

### 可改进点（后续优化）

1. `_search_single` 函数应检查响应体中的 `code` 字段，非 0 时打印错误信息而非静默返回空
2. 对第三方 API 调用的异常分支应有更详细的日志，便于快速定位
3. 可考虑为关键工具调用添加集成测试，在 CI 中验证 API 连通性

# 检索成功但 LLM 未采纳知识库内容

**日期**：2026-06-27  
**关联问题**：修复 `dataset_ids` 后 RAGFlow 能返回数据，但最终回答和 PDF 中不包含知识库信息

---

### 问题现象

修复 `dataset_ids` 参数后，日志显示检索成功：

```
RAGFlow 检索成功, query='北京小众景点', 返回 7 条结果:
【1】北京有一个叫"彩虹胡同"的小众景点，门票 88 元...
```

但最终回答和生成的 PDF 中完全没有出现"彩虹胡同"。**数据取回来了，Agent 却不用。**

---

### 排查过程

#### 第一步：排除检索环节

用 Python 脚本直接调 RAGFlow API，确认 `dataset_ids` 修复后能正常返回数据。日志也显示 `检索成功, 返回 7 条结果`。检索环节没问题。

#### 第二步：分析工具返回值与最终输出的差异

工具 `searchKnowledgeBase` 的返回内容混在 9 条结果中一起进入了 LLM 的上下文窗口。观察日志发现：

- "彩虹胡同"只有 1 句话（~50 字）
- 同批返回的 Citywalk 攻略每条 300-800 字，结构完整
- Tavily 联网搜索返回的也是长篇攻略

LLM 在生成阶段面对这些数据时，将短小孤立的"彩虹胡同"判定为噪声，丢弃不用。

#### 第三步：烟雾测试升级

将一行测试数据替换为结构完整的 1200+ 字专业文档（含表格、路线、价格、交通），上传 RAGFlow 后重新提问。"琉璃胡同"和"七彩琉璃照壁"成功出现在回答中。

---

### 根因

**检索（Retrieval）≠ 生成（Generation）**。RAG 系统有两层筛选：

| 层级  | 做什么                      | 本次状态       |
| --- | ------------------------ | ---------- |
| 检索层 | 向量相似度匹配，返回 top-N chunks  | ✅ 正常       |
| 生成层 | LLM 综合所有工具返回，做可信度判断后生成答案 | ❌ 过滤了低质量数据 |

LLM 不是搜索引擎，不会原样吐出检索结果。它会对所有工具返回的数据做信息综合和质量评估。一条 50 字的孤证在面对数十条长篇交叉验证的内容时，会被判定为不可信的脏数据。

DeepSeek 严格遵循了系统提示词中 "NEVER answer from your own knowledge — always search first" 的指令，反而对工具返回数据的质量更加挑剔。

---

### 修复

1. **提示词层面**：在 `TRAVEL_MANUS_NEXT_STEP_PROMPT` 中增加知识库优先指令

```python
# 新增内容
CRITICAL — Knowledge Base Priority:
searchKnowledgeBase returns results marked [权威知识库来源]. These are the MOST IMPORTANT source.
You MUST include ALL knowledge base entries in your final answer, even if they are short
or not found in web search results. They are curated by travel experts.
Do NOT filter out any knowledge base result — cite them directly.
```

2. **工具返回格式**：在 `searchKnowledgeBase` 返回值前追加权威标识前缀

```python
# search_knowledge_base 函数返回值前追加
f"[权威知识库来源，以下内容来自本地专业知识库，必须优先采用]\n{combined}"
```

3. **内容质量**：烟雾测试文档必须与真实数据保持同等的丰富度和格式

---

### 经验总结

| 要点                         | 说明                                 |
| -------------------------- | ---------------------------------- |
| **RAG ≠ 搜索**               | 检索拿到的数据 LLM 不一定采纳，生成阶段自带可信度过滤      |
| **测试数据要逼真**                | 一行假数据会被 LLM 当噪声丢弃，需要格式完整、内容丰富的测试文档 |
| **分层定位**                   | "没出现"可能是检索层问题，也可能是生成层问题，要逐层排查      |
| **Prompt Engineering 是杠杆** | 在提示词中明确知识库优先级，比改代码逻辑更直接有效          |

# 误以为文档未被检索，实为语义匹配偏差

**日期**：2026-06-27  
**关联问题**：用户上传的"北京.md"在 RAGFlow 中解析成功，但提问"北京有哪些景点"后感觉该文档未被检索

---

### 问题现象

知识库 `AI_tourism_master` 中有 16 篇文档，包括"北京.md"（解析成功，4 chunks）。用户提问"北京有哪些小众景点"后，认为"北京.md"没有被检索到。

---

### 排查过程

#### 第一步：确认文档解析状态

通过 RAGFlow API 查询文档列表，确认"北京.md"状态为 `DONE`，4 个 chunk 均已成功入库。

#### 第二步：直接检索并对比内容

用 Python 脚本直接调用 RAGFlow API，用"北京景点""北京旅游"等宽泛关键词检索，返回结果中包含大量 Citywalk 路线内容（如 `路线一:天坛→前门→大栅栏`、`路线六:北海公园→什刹海`）。

#### 第三步：查看"北京.md"的 chunk 内容

通过 Chunk API 拉取"北京.md"的所有切片，发现内容全是 Citywalk 步行路线，而非景点清单：

```
路线一：天坛→前门→大栅栏→杨梅竹斜街→北京坊
路线二：葫芦巷→白云观→...
路线三：...
```

与检索返回结果完全一致。

---

### 根因

**"北京.md"已经被检索到**，但用户没认出来。根因有两层：

**第一层——用户认知偏差**：用户认为"北京.md"里应该是故宫、天坛、长城等景点清单，实际文档内容是 Citywalk 路线合集。看到检索结果里的"路线一、路线二"时，用户没有将其与"北京.md"关联。

**第二层——语义匹配偏差**：RAGFlow 使用向量语义检索，不是按文件名匹配。"北京景点"的查询向量与 Citywalk"路线"内容的向量有一定相似度但不够高，所以这些 chunk 虽然能被检索到，但排序靠后，被琉璃胡同等更相关的内容挤到了结果末尾。

---

### 经验总结

| 要点                 | 说明                                                 |
| ------------------ | -------------------------------------------------- |
| **RAG 不是文件名搜索**    | 检索基于向量语义相似度，与文档名称无关。文档名叫"北京.md"不代表搜"北京"一定排第一       |
| **Chunk 切分影响检索效果** | 长文档被切成多个独立 chunk 后，检索是对 chunk 而非整篇文档匹配，内容结构直接影响命中率 |
| **内容格式要匹配查询意图**    | 用户搜"景点"，文档写"路线"，语义存在偏差。应该让文档内容结构对齐预期查询方式           |
| **用 Chunk API 验证** | Git 无法判断检索效果时，直接调 API 查看 chunk 内容和检索结果，比猜想要高效      |

# PDF 图片嵌入改造中的连环问题

**日期**：2026-07-10
**项目**：AI 旅游搭子（ai-tourism）
**影响范围**：Agent 生成的 PDF 攻略无法包含目的地图片

---

## 问题一：MCP Server API 版本不兼容

### 问题现象

启用图片搜索 MCP Server 时启动报错：

```
AttributeError: 'Server' object has no attribute 'tool'
```

### 排查过程

检查 `mcp` 库版本，发现已升级到较新版本，底层 API 从低阶 `Server` 重构为 `FastMCP`。

查看 `.venv/Lib/site-packages/mcp/server/__init__.py`，发现导出的是 `FastMCP` 而非原来的 `Server`。`FastMCP` 的装饰器是 `@mcp.tool()`，而旧版 `Server` 需要用 `@server.list_tools()` + `@server.call_tool()` 两段式注册。

### 根因

第三方库向下不兼容的 API 变更。`mcp` 库从 1.x 升级过程中移除了 `Server.tool` 装饰器，改为 `FastMCP` 的声明式 API。

### 修复

将 `image_search_mcp/main.py` 从低阶 Server API 改为 FastMCP：

```python
# 改前（API 已移除）
from mcp.server import Server
from mcp.server.stdio import stdio_server
server = Server("yu-image-search")

@server.tool()
async def search_image(query: str) -> str:
    ...

async def main():
    async with stdio_server() as (read_stream, write_stream):
        await server.run(read_stream, write_stream, ...)

# 改后
from mcp.server.fastmcp import FastMCP
mcp = FastMCP("yu-image-search")

@mcp.tool()
async def search_image(query: str) -> str:
    ...

mcp.run(transport="stdio")
```

### 经验

| 要点                   | 说明                                                             |
| -------------------- | -------------------------------------------------------------- |
| **第三方库的语义版本不等同于稳定性** | `mcp>=1.0` 在 pyproject.toml 中看似稳定，但 1.x 内部仍可能有 breaking change |
| **虚拟环境的依赖版本是最终依据**   | 文档或示例代码可能过时，应以 `.venv/Lib/site-packages/` 中的实际 API 为准          |
| **启动时验证**            | 集成 MCP 子进程的服务应在启动时做冒烟测试，而非在用户请求时才暴露问题                          |

---

## 问题二：LangGraph 递归上限不足导致 Agent 中途崩溃

### 问题现象

Agent 执行到一半报错：

```
Agent SSE error: Recursion limit of 25 reached without hitting a stop condition.
```

### 排查过程

查看运行日志，Agent 完成了 geocode → weather → 多次 searchWeb → RAGFlow，但在生成 PDF 之前崩溃。

分析 `agent/graph.py` 中的图结构：

```
check_state → thinker → actor → thinker → actor → ... → finalize → END
```

每轮循环 = thinker(1次) + actor(1次) = 2 次节点访问。

LangGraph 默认 `recursion_limit = 25`，意味着最多执行 25 个节点。check_state 占 1 次，剩余 24 次 ≈ 12 轮循环。而完整的 9 步流程需要更多轮次（每一步都需要 thinker 决策 + actor 执行，部分步骤还需多次搜索），12 轮远远不够。

### 根因

LangGraph 的 `recursion_limit` 与业务上的 `max_steps` 是两个不同的概念：

| 参数                | 默认值 | 含义                         |
| ----------------- | --- | -------------------------- |
| `recursion_limit` | 25  | LangGraph 引擎执行节点的**物理上限**  |
| `max_steps`       | 20  | AgentState 中记录的**业务步数计数器** |

`max_steps=20` 只是个计数器，实际由 `_should_continue` 和 `_should_loop` 中的判断逻辑消费。而 `recursion_limit` 是硬限制——到达即抛异常，不管业务逻辑是否认为已完成。

### 修复

在调用 `astream_events` 时显式设置 `recursion_limit`：

```python
# 改前
agent_graph.astream_events(initial_state, version="v2")

# 改后
agent_graph.astream_events(initial_state, version="v2", config={"recursion_limit": 100})
```

提高到 100 后，Agent 可以跑约 50 轮，远超过 `max_steps=20` 的需求。

### 经验

| 要点                             | 说明                                                           |
| ------------------------------ | ------------------------------------------------------------ |
| **LangGraph 的两个限制参数各管各的**      | `recursion_limit` 是引擎级硬限制，`max_steps` 是业务逻辑计数器。修改业务逻辑时容易忽略前者 |
| **默认值 25 适合简单交互，不适合多工具 Agent** | 每轮循环消耗 2 次递归，20 步的 Agent 需要 40+ 的递归上限                        |
| **异常信息有明确的解决指引**               | LangGraph 的错误提示直接建议了 `recursion_limit` 配置项，应优先阅读而非绕路排查       |

---

## 问题三：Agent 调用 do_terminate 后陷入死循环

### 问题现象

Agent 完成所有工作后，反复执行"检查进度→do_terminate→检查进度→do_terminate..."，日志中同一段输出出现 10+ 次。

### 排查过程

查看 `_should_loop` 函数：

```python
def _should_loop(state: AgentState) -> str:
    if state.get("agent_state") in ("FINISHED", "ERROR"):
        return "finalize"
    if state.get("current_step", 0) >= state.get("max_steps", 20):
        return "finalize"
    return "thinker"  # ← 始终返回 thinker
```

当 ToolNode 执行完 `do_terminate` 后，ToolMessage(content="任务结束") 被追加到消息列表。但 `_should_loop` 只检查了 `agent_state` 和 `current_step`——两者都未改变——于是无条件返回 `"thinker"`。

LLM 再次被调用，看到消息列表中已有"任务结束"的结果，检查进度清单发现全 ✅，于是再次调用 `do_terminate`，形成死循环。

 死循环的形成路径：

1. thinker → 调用 do_terminate
2. _should_continue 检测到 tool_calls → 路由到 "actor"
3. ToolNode 执行 do_terminate → ToolMessage(content="任务结束") 追加到消息列表
4. _should_loop 开始判断：
   - agent_state == "RUNNING"     → 不触发终止（改状态的唯一地方是 _finalize 节点）
   - current_step < max_steps     → 不触发终止（step 只在 thinker 节点 +1）
   - 消息内容检查 → 【修复前不存在这个分支】
5. 三个条件全不命中 → 无条件返回 "thinker"
6. LLM 看到进度清单全 ✅ → 再次调用 do_terminate → 回到步骤 1

### 根因

**循环终止条件缺失。** do_terminate 只是一个普通工具，return 了一个字符串 "任务结束"，LangGraph 把它包装成 ToolMessage塞进消息列表就完事了。它没有能力去修改 AgentState 里的 agent_state字段——改状态只能靠节点函数（_finalize）返回字典。而进入 _finalize 的路又被 _should_loop 堵死了，因为 _should_loop不知道该结束。

### 修复

现在的修复就是在 _should_loop 里加了对最后一条消息内容 "任务结束"的字符串检测——这是个实用但脆弱的补丁：依赖工具返回的魔法字符串来路由，而不是靠状态机自身语义更稳健的做法是让_should_loop 也检查"上一条 AIMessage 是否调用了 do_terminate"（通过 tool_calls 里的 name 判断），不依赖返回内容。

在 `_should_loop` 中检测 `do_terminate` 的返回值：

```python
def _should_loop(state: AgentState) -> str:
    if state.get("agent_state") in ("FINISHED", "ERROR"):
        return "finalize"
    if state.get("current_step", 0) >= state.get("max_steps", 20):
        return "finalize"
    # 新增：do_terminate 已执行 → 结束
    messages = state.get("messages", [])
    if messages:
        last = messages[-1]
        if hasattr(last, "content") and isinstance(last.content, str) and "任务结束" in last.content:
            return "finalize"
    return "thinker"
```

原理：`do_terminate` 的返回值是固定的字符串 `"任务结束"`。ToolNode 执行后将其作为 ToolMessage 追加到消息末尾。检查最后一条消息的内容即可判断是否需要结束。

### 经验

| 要点                         | 说明                                                                 |
| -------------------------- | ------------------------------------------------------------------ |
| **工具调用的"副作用"必须被检查**        | 调用 `do_terminate` 的目的是触发结束，但结束逻辑不在工具本身而在路由函数中——调用方和决策方分离，容易遗漏      |
| **图的条件边需要有完整的终止条件集**       | `actor → ?` 的路由不仅需要考虑"是否超步数""是否出错"，还需要考虑"工具本身是否要求结束"               |
| **恒真回路是 StateGraph 的常见陷阱** | 只要条件边所有分支都不导向 `finalize`，且 `current_step` 未超限，Actor→Thinker 就会无限循环 |

---

## 问题四：Agent 不断自我怀疑、反复重启工作流

### 问题现象

Agent 完成一轮工作后，不进入 PDF 生成和终止，而是调用 `read_file` 读取之前生成的内容，然后说"重新完整执行所有步骤"，把 geocode、searchWeb 等全部重做一遍。

### 排查过程

查看 `TRAVEL_MANUS_NEXT_STEP_PROMPT` 的内容：

```
CRITICAL: You MUST call multiple tools before answering.
Required steps for ANY travel question:
- Step 1: geocode...
- Step 2: searchWeb...
...
- Step 8: generatePDF...
- Step 9: doTerminate
```

这个提示词在每一轮 thinker 执行前都被注入。LLM 每次看到"Required steps for ANY travel question"和完整的 9 步列表，就会认为当前进度不够、应该从头执行。

同时，提示词中的强力措辞（CRITICAL、MUST、Required）使 LLM 倾向于过度执行而非适时终止。

### 根因

**提示词中的工作流描述从"参考"变成了"命令"。** 每次注入全部步骤导致两个问题：

1. LLM 认为这些步骤尚未执行（认知错觉）
2. 即使部分步骤已完成，LLM 仍然倾向于重新执行以确保完整

### 修复

将 `NEXT_STEP_PROMPT` 从完整的步骤列表改为**进度检查清单**：

```python
# 改前：每次注入完整步骤列表
TRAVEL_MANUS_NEXT_STEP_PROMPT = """\
CRITICAL: You MUST call multiple tools before answering.
Required steps for ANY travel question:
- Step 1: geocode...
- Step 2: searchWeb...
...
- Step 8: generatePDF...
- Step 9: doTerminate
You MUST generate a PDF before terminating."""

# 改后：进度检查清单，引导继续而非重启
TRAVEL_MANUS_NEXT_STEP_PROMPT = """\
Continue from where you left off. Do NOT restart or repeat tools you have already called.

Check your progress:
- ✅ Weather info gathered?
- ✅ Attractions info gathered?
- ✅ Food & transportation info gathered?
- ✅ Image URLs collected?
- ✅ PDF generated?

If ALL the above are done, call doTerminate immediately.
If most info is gathered but you're missing images, just call searchImage once, then generatePDF.
If you have some info but gaps remain, call only the specific tool to fill the gap — do NOT redo completed steps."""
```

系统提示词中的工作流也从"WORKFLOW: 1-9 必须按顺序执行"改为"SUGGESTED WORKFLOW (guide, not checklist)"。

### 经验

| 要点                         | 说明                                                     |
| -------------------------- | ------------------------------------------------------ |
| **NEXT_STEP_PROMPT 的设计哲学** | 不是让 LLM 重启，而是让 LLM 续继。提示词应反映"现在几点钟了"而非"第一天应该做什么"       |
| **措辞强度影响执行行为**             | MUST/CRITICAL/Required 会让 LLM 产生"安全错觉"——多做总比少做好，导致反复执行 |
| **进度清单比步骤列表更适合循环场景**       | 清单让 LLM 评估已做 vs 未做，步骤列表让 LLM 觉得"还没开始"                  |

---

## 问题五：Pexels 图片搜索中文城市返回无关图片

### 问题现象

搜索"天津"的图片时，PDF 中出现了哈尔滨、上海等其他城市的图片。

### 排查过程

查看 Pexels API 的请求参数。Pexels 是一个以英文标签为主的海外图库，中文关键词的语义匹配非常差。搜索"天津"时，API 找不到足够的相关图片，于是返回其他中国城市的照片"凑数"。

### 根因

Pexels 的图像标签以英文为主，中文关键词的语义覆盖严重不足。这是一个**上游数据源的语言偏向**问题，无法通过调整 API 参数彻底解决。

### 修复方案

采用三层兜底的搜索词策略：

```python
# 第1层：LLM 主动提供英文关键词
query_en = "Tianjin China"  # LLM 在调用时传入

# 第2层：内置城市中英映射表
_CITY_EN_MAP = {
    "北京": "Beijing China",
    "上海": "Shanghai China",
    "天津": "Tianjin China",
    # ...共 26 个热门旅游城市
}

# 第3层：中文检测兜底
if query contains Chinese characters:
    search_query = f"{query} China"  # 未映射的中文城市加 China 后缀

# 优先级：query_en > 映射表 > query + "China" > 原始query
```

### 方案选型权衡

在实现过程中考虑过三种方案：

| 方案                 | 优点         | 缺点             |
| ------------------ | ---------- | -------------- |
| A. 工具内嵌映射表         | 可靠，不依赖 LLM | 需要维护映射表        |
| B. LLM 提供 query_en | 灵活，无需维护    | 依赖 LLM 的记性，不可靠 |
| C. 换百度图片API        | 中文支持最好     | 需要额外申请百度开发者账号  |

最终选择 **A+B 双重保险**：LLM 如果主动传 `query_en` 优先使用，否则查内置映射表，映射表没有且含中文时自动追加"China"后缀。

### 经验

| 要点                   | 说明                                           |
| -------------------- | -------------------------------------------- |
| **不要假设海外 API 支持中文**  | Pexels 支持中文搜索"有结果"，但不代表结果准确。功能可用 ≠ 质量达标      |
| **三层兜底架构**           | 用户输入 → LLM 处理层 → 代码逻辑层 → 兜底层，每层解决上一层的遗漏      |
| **不要依赖 LLM 的"记性"**   | 即使 prompt 写了明确的指令，LLM 也可能忽略。关键逻辑应写在代码层而非提示词层 |
| **字段是否存在 ≠ 字段值是否准确** | 图片搜索能返回结果不代表结果相关。测试时应检查内容质量而非仅检查返回状态         |

# 检索前优化：查询扩展与查询改写

**日期**：2026-07-10
**项目**：AI 旅游搭子（ai-tourism）
**涉及文件**：`src/rag/query_expander.py`、`src/agent/prompts.py`、`src/api/router.py`

---

## 背景

RAG（Retrieval Augmented Generation）系统的检索质量直接决定生成质量。用户输入的原始查询往往太短、太模糊，导致向量检索和搜索引擎召回的内容相关性不足。本项目实现了两种**检索前优化（Pre-Retrieval Optimization）** 策略：查询扩展和查询改写。

---

## 策略一：查询扩展（Query Expansion）

### 实现

```python
EXPANSION_ASPECTS = ["景点", "美食", "交通", "住宿", "天气", "攻略", "门票", "文化"]

def expand_query(query: str) -> list[str]:
    expanded = [query]
    for aspect in EXPANSION_ASPECTS:
        expanded.append(f"{query} {aspect}")
    return expanded[:5]
```

**位置**：`src/rag/query_expander.py`
**调用时机**：`src/tools/ragflow_search.py` 中 `search_knowledge_base` 工具内部，在调用 RAGFlow API 之前

### 工作原理

```
原始查询: "北京"

                ↓  expand_query()

子查询1: "北京"           → RAGFlow → 通用文档
子查询2: "北京 景点"      → RAGFlow → 景点相关文档
子查询3: "北京 美食"      → RAGFlow → 美食相关文档
子查询4: "北京 交通"      → RAGFlow → 交通相关文档
子查询5: "北京 住宿"      → RAGFlow → 住宿相关文档

                ↓  去重合并

最终结果：5个维度的综合知识库内容
```

### 关键设计

- **内容去重**：使用 `seen` set 对 5 个子查询的结果做行级去重，避免重复内容塞满上下文窗口
- **结果截断**：最终合并结果截取前 3000 字符，防止工具返回值过大
- **兜底机制**：所有子查询均无结果时，用原始查询再试一次

### 为什么有用

RAGFlow 知识库中的文档按不同维度组织——一篇讲"北京美食"，一篇讲"北京交通"。只用"北京"一个词去向量检索，可能只召回最通用的文档，遗漏特定维度的内容。多查询扩展从多个角度分别检索，合并结果，提高召回率。

---

## 策略二：查询改写（Query Rewriting）

### 实现

```python
# prompts.py
QUERY_REWRITE_PROMPT = """\
你是一个旅游查询改写助手。将用户模糊的旅行问题改写成清晰、完整、适合搜索引擎检索的查询语句。
规则：
- 补充缺失的关键信息（目的地、美食、景点、交通、天气等维度）
- 保持原意，不要编造不存在的信息
- 直接输出改写后的查询，不要任何解释、前缀、引号或额外内容
- 如果问题已经清晰完整，原样返回
"""

# router.py — API 入口处调用 DeepSeek 改写
rewrite_msgs = [
    SystemMessage(content=QUERY_REWRITE_PROMPT),
    HumanMessage(content=message),
]
rewrite_response = await chat_model.ainvoke(rewrite_msgs)
rewritten = (rewrite_response.content or "").strip() or message
```

**位置**：`src/api/router.py` 第 73-80 行（`/travel/agent/chat` 端点）
**调用时机**：用户请求到达 Agent 之前，`build_initial_state()` 之前

### 工作原理

```
用户输入: "北京"

        ↓ DeepSeek 改写

改写后: "北京旅游攻略：必去景点推荐、特色美食、最佳旅游季节、交通出行建议"

        ↓ 传给 Agent 作为初始 HumanMessage

Agent 用改写后的查询调用: searchWeb、searchKnowledgeBase、geocode 等工具
```

### 改写效果示例

| 用户输入       | 改写后                                |
| ---------- | ---------------------------------- |
| "北京"       | "北京旅游攻略：必去景点推荐、特色美食、最佳旅游季节、交通出行建议" |
| "成都有什么好吃的" | "成都美食推荐：本地人必吃的特色小吃、火锅店、川菜馆和美食街"    |
| "三亚亲子游"    | "三亚亲子旅游攻略：适合孩子的景点、酒店推荐、交通指南"       |
| "上海迪士尼攻略"  | "上海迪士尼攻略：门票价格、游玩路线、住宿推荐、交通方式"      |

### 关键设计

- **SSE 透传**：改写结果通过 `[REWRITE] 改写内容` 事件发给前端，用户可以看到自己的问题被如何优化
- **空结果兜底**：如果 LLM 返回空内容，自动回退到原始查询
- **复用同一模型**：使用和 Agent 相同的 DeepSeek 模型，不需要额外引入改写模型

---

## 两种策略的对比

| 维度           | 查询扩展 (Query Expansion) | 查询改写 (Query Rewriting) |
| ------------ | ---------------------- | ---------------------- |
| **实现方式**     | 字符串拼接 + 固定维度列表         | LLM 调用，语义理解后重写         |
| **是否需要 LLM** | 否，纯代码逻辑                | 是，每次请求多一次 LLM 调用       |
| **目标**       | 从多个维度检索，提升召回率          | 让模糊问题变清晰，提升语义匹配度       |
| **适用场景**     | 知识库（RAGFlow）向量检索       | 搜索引擎（Tavily）和知识库通用     |
| **输入**       | 短关键词（城市名）              | 任意自然语言问题               |
| **输出**       | 多个子查询                  | 一个优化后的查询               |
| **延迟开销**     | 几乎为零（字符串拼接）            | ~1-2 秒（一次 LLM 调用）      |
| **效果上限**     | 受限于固定维度列表              | 受限于 LLM 的理解能力          |

---

## RAG 检索前优化的完整链路

```
用户输入: "北京"

    ↓ 查询改写（DeepSeek）

重构后: "北京旅游攻略：必去景点..."

    ↓ build_initial_state()

Agent 开始执行

    ↓ searchKnowledgeBase 工具内部

查询扩展: ["北京", "北京 景点", "北京 美食", "北京 交通", "北京 住宿"]
         ↓
    RAGFlow API × 5 次调用
         ↓
    去重合并 → 返回给 Agent

    ↓ searchWeb (Tavily)

直接用改写后的完整查询搜索互联网

    ↓ 其他工具...

最终回答 + PDF 生成
```

### 为何分层设计

1. **改写在前、扩展在后**：改写优化的是"用户问什么"，扩展优化的是"知识库怎么查"。两者解决不同层次的问题
2. **扩展只用于知识库**：Tavily 搜索引擎本身对长查询理解更好，不需要多查询扩展；RAGFlow 向量检索对短查询敏感，适合扩展
3. **改写依赖 LLM 但扩展不依赖**：改写需要语义理解，必须用 LLM；扩展是纯字符串操作，零延迟

---

## 经验总结

| 要点                     | 说明                                                                      |
| ---------------------- | ----------------------------------------------------------------------- |
| **检索前优化比检索后优化更高效**     | 在查询阶段提升输入质量，比检索后做重排序（Re-ranking）更轻量，延迟更低                                |
| **改写 vs 扩展：两种不同的优化思路** | 改写是从"模糊→清晰"，扩展是从"单点→多维"。可以组合使用，但解决的是不同问题                                |
| **改写对延迟敏感**            | 多了一次 LLM 调用，Streaming 模式下用户体验影响不大（用户先看到 `[REWRITE]` 事件，然后看到 Agent 开始工作） |
| **扩展成本可控**             | 固定 5 个子查询，且只对知识库检索生效，避免对每个搜索引擎都做多查询放大                                   |
| **知识库内容结构影响扩展效果**      | 如果知识库文档没有按"景点""美食"等维度组织，扩展带来的收益会降低。扩展策略应与知识库内容结构对齐                      |

# RAGflow如何提高文档召回率

## 面试官提问：你是怎么提高召回率的？

RAGFlow 提高召回率可以从以下几个维度入手：

  ---

1. 分块策略调优
   这是影响召回最直接的因素。RAGFlow 支持多种分块方法，中文旅游文档建议按段落分块 + 200~500 token 块大小 + 10%~20%
   重叠。块太大会稀释语义导致相似度评分偏低，太小则丢失上下文导致检索结果碎片化。重叠保证"故宫门票旺季60元"这种跨边界信息
   不会被切断。

2. Embedding 模型选型
   默认模型不一定适合中文场景。旅游文档涉及景点名、地名、文化术语，中文语义模型（如 bge-large-zh-v1.5、m3e-large、text2ve
   c-large-chinese）对中文短句和专有名词的语义捕捉远好于多语言通用模型，换模型往往能带来 10~20% 的召回提升。

3. 开启混合检索
   纯向量检索对精确关键词（"琉璃胡同""狗不理包子"）可能漏召回，因为向量偏向语义相似而非字面匹配。RAGFlow 支持 BM25 关键词
   
   + 向量语义的混合检索，结合二者优势，关键词命中 + 语义泛化互补。

4. 引用重排序
   检索返回的 top_k 条结果中排前面的不一定最相关。RAGFlow 支持接入 Reranker 模型（如
   bge-reranker-large）对粗召回结果二次精排，将真正相关的结果提到前面，有效提升 top-N 命中率。

5. 保证文档解析质量
   RAGFlow 的 DeepDoc 解析依赖文档本身格式。PDF 如果是扫描件、表格混排、图文混排，解析出的文本质量差，后续分块索引都没有
   意义。上传前确保文档文字清晰、结构干净，必要时先用 Markdown 重排版再导入。

6. 查询端增强（代码层已做）
   你项目里已经实现的 Query Rewrite + 多查询扩展是 RAG 召回的标配优化，属于不碰知识库配置就能见效的手段。
   
   ---
   
   优先级建议：先检查当前分块大小和 Embedding 模型 → 开启混合检索 → 加 Reranker。这三步改动最小、收益最大。

# 终止判定依赖返回值文案，导致死循环与误终止

**日期**：2026-09-17  
**项目**：AI 旅游搭子（ai-tourism）  
**影响范围**：Agent 循环无法可靠结束——终止工具与其他工具同轮调用时停不下来（空转到超步数），普通工具正文恰好含"任务结束"时又提前终止  
**发现方式**：源码审查 + 单元测试构造复现。属潜伏缺陷，非线上故障；两个现象在当前提示词下出现概率不高，但一旦触发即为静默行为错误

---

### 问题现象

两类方向相反的现象，来自同一个判据：

**现象一：该停不停。** 一轮里同时调用了终止工具和其他工具（如"终止 + 生成 PDF"），循环没有结束，继续调用模型，直到 `max_steps=20` 才被强制断掉。

**现象二：不该停却停了。** 只调用了知识库检索工具，Agent 却直接结束。检查检索返回的正文，里面有一句"……半日游到此任务结束，可顺路去王府井"——攻略文案里的一句普通话。

两者的共同点是：**没有报错，没有日志**。一个表现为空转到步数上限，另一个表现为提前收工，从外部看到的都只是"结果不对"。

---

### 排查过程

#### 第一步：定位判据

`_should_loop` 决定 actor 执行完之后是回 thinker 继续，还是去 finalize 结束。它的终止条件之一写的是：

```python
last = messages[-1]
if "任务结束" in last.content:
    return "finalize"
```

判据是**消息正文里的字符串**，而"任务结束"这四个字正是终止工具的返回值。

#### 第二步：构造复现——顺序依赖

终止工具的实现就是返回一个固定字符串：

```python
@tool(...)
def do_terminate() -> str:
    return "任务结束"
```

判断只看 `messages[-1]`。按 [终止工具, 生成 PDF] 的顺序构造一轮工具结果后，最后一条是生成 PDF 的结果——里面没有"任务结束"，于是返回 `thinker`，循环继续。

**现象一复现。**

#### 第三步：构造复现——误命中

反过来构造：只调用知识库检索工具，让返回正文里包含"任务结束"四个字。判断命中，返回 `finalize`——**明明没有任何工具要求结束**。

**现象二复现。**

#### 第四步：确认是否存在更可靠的判据

工具结果对象（ToolMessage）本身带 `name` 字段，记录这条结果由哪个工具产出。用 `do_terminate.name` 与之比对即可，完全不需要读正文。先用一条命令确认两个名字可对齐：

```
terminate tool .name -> 'do_terminate'
ToolMessage.name     -> 'do_terminate'
```

---

### 根因

**工具和决策方被拆开了，中间只连着"返回值字符串"这一根传话的绳子。**

- 终止工具想表达"该结束了"，但它**没有能力改图的状态**——改状态只能靠节点函数返回一个字典。
- 它唯一的流出通道就是返回值，所以只能把这层意思塞进返回的文本里。
- 决策方（`_should_loop`）有权结束，但它看不到"谁调用了什么"，只能去文本里猜。

三个缺陷都长在这一根绳子上：

| 缺陷   | 表现             | 原因                   |
| ---- | -------------- | -------------------- |
| 文案依赖 | 改返回值文案即失效，且不报错 | 判据是 `"任务结束"` 这个字面量   |
| 顺序依赖 | 同轮多工具时停不下来     | 只看 `messages[-1]` 一条 |
| 误命中  | 普通正文含这四个字就提前终止 | 内容匹配分不清"谁说的"         |

**同一文件里就有反面对照**：`_should_continue` 判断"要不要去执行工具"用的是 `hasattr(last, "tool_calls") and last.tool_calls`——看**结构**。两种判据并存，脆的那个恰好落在终止路径上。

---

### 修复

**文件**：`src/agent/graph.py`

**改动**：新增按工具名匹配的辅助函数，`_should_loop` 改用它，删除内容匹配。

```python
def _terminate_was_called(messages: list) -> bool:
    for message in reversed(messages):
        if not isinstance(message, ToolMessage):
            break
        if message.name == do_terminate.name:
            return True
    return False


def _should_loop(state: AgentState) -> str:
    if state.get("agent_state") in ("FINISHED", "ERROR"):
        return "finalize"
    if state.get("current_step", 0) >= state.get("max_steps", 20):
        return "finalize"
    if _terminate_was_called(state.get("messages", [])):
        return "finalize"
    return "thinker"
```

三个要点：

1. **按工具名判，不按内容判** —— 读 `ToolMessage.name` 这个结构化字段。
2. **从尾部回扫到上一条 AI 消息为止** —— 判定范围是"本轮所有工具结果"，不是只看最后一条。
3. **工具名取自 `do_terminate.name`，不写字面量** —— 名字从工具对象本身来。终止函数即使被改名，图这边自动跟着变，不会静默失效。

**文件**：`tests/test_agent.py`

**改动**：从 1 条测试增至 7 条。新增 6 条路由测试，其中 3 条专门复现上述缺陷，按"先写、先看它失败、再改代码"的顺序落地：

| 测试                | 修复前                | 修复后        |
| ----------------- | ------------------ | ---------- |
| 终止工具结果不在最后一条      | 返回 `thinker`（停不下来） | `finalize` |
| 终止工具返回文案改为 `done` | 返回 `thinker`（静默失效） | `finalize` |
| 普通工具正文含"任务结束"     | 返回 `finalize`（误终止） | `thinker`  |

另 3 条锁住原有出口：正常继续、超步数结束、终态结束。

```
9 passed in 0.45s
```

---

### 经验总结

| 要点                  | 说明                                                              |
| ------------------- | --------------------------------------------------------------- |
| **工具没有状态表达权**       | 工具跑完只能靠返回值说话，判断方只能从返回值里猜。凡是"工具要求结束 / 要求跳转"这类需求，都会退化成字符串约定       |
| **人类可读文案不是协议**      | 用中文提示语当判据，等于把界面文案升级成了隐藏协议：改文案 = 改协议，且不报错                        |
| **"谁说的"比"说了什么"更可靠** | ToolMessage 的 `name` 是结构化来源标记；内容匹配无法区分工具身份，也无法区分工具说的话和它引用的资料    |
| **只取最后一条是隐含假设**     | `messages[-1]` 假设了"一轮只有一条结果"。模型一旦并行调用多个工具，假设立刻不成立               |
| **可复现的缺陷先写成测试**     | 三个现象都能用几十行构造出来。写成测试后，修复前后的行为差异一目了然，且能防退化                        |
| **名字要取自源头，不能复述**    | 工具名从 `do_terminate.name` 取，而不是再手写一遍 `"do_terminate"`，否则改名时两处不同步 |

---

### 可改进点（后续优化）

1. **根治：让工具直接携带路由指令。** 当前环境（langgraph 1.2.6 + langgraph-prebuilt 1.1.0）的 ToolNode 已支持工具返回 `Command` 对象，其中可同时装载状态更新与跳转目标。终止工具执行时即可自行把状态置为终态并指定去向，`_should_loop` 里的判定整段删除，上述三个缺陷一并消失。落地前需实测两种边界：一轮内多个工具同时返回 `Command` 时的合并行为，以及 `Command.update` 的字段约束。
2. **同类债：检索工具的字符串匹配。** `search_knowledge_base` 与 `_search_single` 用 `"未找到"` / `"失败"` / `"出错"` 判断检索是否成功，是同一个病——应改为检查 RAGFlow 响应体里的业务错误码字段，这同时也能修掉此前"业务错误码被静默丢弃"的遗留问题。
3. **路由函数仍是测试覆盖最薄的地方。** 本文件记录的多数控制流问题都落在这两个函数上，目前仅覆盖到终止判定；`_should_continue`、`thinker` 的 `next_step_prompt` 注入、以及 `recursion_limit` 与 `max_steps` 的配比关系均无测试。

# 所有请求走同一条重流程：引入意图识别分流

**日期**：2026-09-17
**项目**：AI 旅游搭子（ai-tourism）
**涉及文件**：`src/agent/intent.py`（新增）、`src/agent/state.py`、`src/agent/graph.py`、`src/agent/prompts.py`、`src/tools/__init__.py`、`src/api/router.py`、`src/web/templates/travel.html`
**设计文档**：`docs/superpowers/specs/2026-09-17-intent-recognition-design.md`

---

## 背景与问题

`travel_agent_chat` 把所有用户输入无差别推进同一条 ReAct 循环，而该循环用的 system prompt 把输出契约写死了：

```
OUTPUT REQUIREMENTS:
Your final answer MUST be a COMPREHENSIVE travel guide (at least 700 Chinese characters) covering:
...
After your text answer, call generatePDF with the FULL guide content and image URLs, then call doTerminate.
```

于是用户问"天津今天天气怎么样"，甚至只说一句"你好"，也会被要求产出 700 字攻略并生成 PDF。**系统里没有"这个问题不该走重流程"的判断点。**

同时，意图判断是**隐式**的——藏在 `thinker` 节点的工具选择里（模型自己决定调哪个工具）。它不可观测、不可控制，出问题时只能从"模型调了什么工具"反推。

改造目标三条：区分意图让轻请求不再被重流程污染；让意图变成显式可观测的状态；**不改变 `plan_trip` 的现有行为**。

---

## 方案选型

先分意图粒度：**深度规划 `plan_trip` / 单点问答 `quick_query` / 闲聊 `chitchat`**。三类语义清晰、边界好判，且"单点问答"内部不混杂，后续想给它单独接工具时不用返工。

再定分类落在哪一层，三个候选：

| 方案       | 做法                              | 结论     |
| -------- | ------------------------------- | ------ |
| 一、前置分类器  | 在 router 入口分类，再决定走哪条路           | **选中** |
| 二、图内分类节点 | 在 graph 前端插 `classify` 节点，条件边分叉 | 未选     |
| 三、纯提示词分流 | 不分类，把三类判断和行为全写进 system prompt   | **排除** |

**排除方案三的理由值得记下**：它看着最省，但那正是当前的现状。现在提示词里就写着"必须 700 字 + 必须出 PDF"，结果就是靠模型自觉、不可靠。再加一段"如果是闲聊就别生成 PDF"不会比现在更可靠——**约束靠提示词自觉，本身就是问题的一部分**。

**选方案一 + 借用方案二的一个优点**：分类结果写进 `AgentState`。

- 方案二改的是核心控制流，而这块刚在《终止判定依赖返回值文案》里踩过坑，能不动就不动
- 前置拦截还能让闲聊和单点问答**根本不进 ReAct 循环**，省掉一整轮工具往返
- 但方案二的"意图进 state、可观测"确实有用，且代价只是 `AgentState` 加一个字段，白拿

---

## 实现

### 一、分类的产生：合并进原有的改写调用

`router.py` 原本每次请求已有一次额外模型调用做查询改写。把这轮扩展为同时输出意图和改写后的查询：

```python
class QueryIntent(BaseModel):
    intent: Literal["plan_trip", "quick_query", "chitchat"]
    query: str

async def classify_and_rewrite(chat_model, user_message: str) -> tuple[str, str]:
    ...
```

**净增延迟为零**——那轮调用本来就要花。

### 二、结构化输出：必须显式指定 method

这是本次最容易踩的坑，先写探针实测过，结论如下：

| method               | 实测结果                                                                                                                  |
| -------------------- | --------------------------------------------------------------------------------------------------------------------- |
| 默认（不传）               | **失败**。langchain 解析成 `json_schema` 型 `response_format`，DeepSeek 返回 400 `This response_format type is unavailable now` |
| `json_mode`          | 可达但**不可用**。**无枚举约束**，实测让它分类"你好"，返回 `{"intent": "greeting"}` —— 凭空造出 schema 外的值，Pydantic 校验直接失败                        |
| `"function_calling"` | **可用**。schema 作为工具定义下发，枚举被约束，校验通过                                                                                     |

所以必须写死：

```python
structured = chat_model.with_structured_output(QueryIntent, method="function_calling")
```

不传 `method` 会在 DeepSeek 上直接 400。这条已写成测试锁住。

### 三、兜底：本次改造最重要的安全属性

以下任一情况，一律回落 `("plan_trip", 原文)`：

- 模型调用抛异常
- 结构化解析 / Pydantic 校验失败
- 返回的 `query` 为空且意图不是 `chitchat`
- 意图值不在枚举内（实测模型确实会返回 `greeting` 这类值）

即**最坏情况等于本次改造没做**。这保证了改动不会让线上行为比改造前更差。

### 四、三条路径

| 意图            | 工具集                                                                               | max_steps | 结束方式                 |
| ------------- | --------------------------------------------------------------------------------- | --------- | -------------------- |
| `plan_trip`   | 全部                                                                                | 20        | `do_terminate` 或自然收尾 |
| `quick_query` | `geocode` / `get_weather` / `search_web` / `search_knowledge_base` / `scrape_web` | 6         | 只能自然收尾               |
| `chitchat`    | 无                                                                                 | —         | 单次调用直接结束             |

**工具裁剪为什么便宜**：`bind_tools` 和 `ToolNode` 都是编译期固定的，运行期改不了。但 `router.py` 已经是**每请求重建图**，所以按意图裁工具只需给这次构建传子集，`graph.py` 的控制流一行不用改，也不必维护多张图。

**`quick_query` 为什么不需要终止工具**：`_should_continue` 已有一条自然收尾路径——模型给出最终文本回答时不带 `tool_calls`，这条边就走 `finalize`：

```python
if hasattr(last, "tool_calls") and last.tool_calls:
    return "actor"
return "finalize"
```

所以裁掉 `do_terminate` 后循环仍能正常结束，`_should_loop` 的终止判定完全不受影响。这说明**上一轮把终止判定改成结构化匹配是有回报的**：这里换工具集，那边不用跟着动。

### 五、状态变更：控制流一字未动

```python
class AgentState(TypedDict):
    ...
    intent: str  # plan_trip | quick_query | chitchat
```

`build_initial_state(user_message, intent="plan_trip")` 按意图选 `system_prompt`、`next_step_prompt`、`max_steps`。**默认值就是当前行为**，老调用方不传参保持原样。

`graph.py` 的节点、边、两个条件函数**没有任何修改**。

---

## 实测结果

起真实服务跑通三条路径：

```
你好             → [INTENT] chitchat     → 无工具、无 PDF，53 字正常对话
天津今天天气      → [INTENT] quick_query  → geocode → get_weather → 53 字回答，无 PDF
帮我规划天津三日游  → [INTENT] plan_trip   → search_web/kb/geocode/weather → generate_pdf → do_terminate
```

单点问答的回答是 **53 字，不含 PDF / 攻略 / 行程字样**；改造前它会产出一篇 700 字攻略加一个 PDF——这就是全部收益。

`plan_trip` 的工具调用序列与改造前一致，无回归。

---

## 附带修复一：提示词里的工具名全部对不上

### 问题现象

本次顺手加了一个一致性测试，拿来跑**改造前**的提示词，直接红：

```
AssertionError: TRAVEL_MANUS_SYSTEM_PROMPT 引用了不存在的工具:
{'searchWeb', 'searchKnowledgeBase', 'generatePDF', 'doTerminate',
 'getWeather', 'scrapeWeb', 'fileOperation', 'searchImage'}
```

### 根因

提示词用驼峰名，实际注册名是下划线：

| 提示词里写的                | 实际注册名                                  |
| --------------------- | -------------------------------------- |
| `searchWeb`           | `search_web`                           |
| `searchKnowledgeBase` | `search_knowledge_base`                |
| `generatePDF`         | `generate_pdf`                         |
| `doTerminate`         | `do_terminate`                         |
| `getWeather`          | `get_weather`                          |
| `scrapeWeb`           | `scrape_web`                           |
| `searchImage`         | `search_image`                         |
| **`fileOperation`**   | **不存在**，实际是 `read_file` / `write_file` |

**8 个工具名，一个都对不上。** 模型一直在靠语义猜工具名——好在 DeepSeek 的 function calling 是按 schema 匹配的，"说什么名字"只在提示词的引导层面起作用，所以表现为"引导失效"而非"调用失败"，因此长期没被发现。

### 修复

重写三份提示词时一并改成下划线名，删掉 `fileOperation`，拆成 `read_file` / `write_file` 两条。并加测试守住：

```python
def test_every_tool_listed_in_a_prompt_actually_exists(prompt_name):
    ghost_tools = _mentioned_tool_names(prompt) - REAL_TOOL_NAMES
    assert ghost_tools == set()
```

配套两条防"假绿"：一条确认扫描器确实能扫到工具名，一条确认提示词里确实列了工具。

### 一个副产品：MCP 工具名无法静态枚举

`amap-maps` 的工具名由第三方 npm 包 `@amap/amap-maps-mcp-server` 在运行期提供，本项目文档里没有记录，单测无法枚举。处理方式是**重写后的提示词不引用 amap-maps 的任何工具**——它们与本地 `geocode` / `get_weather` 功能重叠，指向本地版本即可。这样一致性测试完全静态可判定，不依赖启动 MCP 子进程。

---

## 附带修复二：`[REWRITE]` 一直在往界面上漏原始文本

### 问题现象

前端 `onmessage` 只认三个前缀：

```javascript
if (data === '[DONE]') { ... }
if (data.startsWith('[TOOL]')) { ... }
if (data.startsWith('[ERROR]')) { ... }
```

**没有 `[REWRITE]`。** 于是这行落进 `else`，`JSON.parse` 失败，被 `catch` 当成正文拼进气泡：

```javascript
try {
    const text = JSON.parse(data);
    this.buffer += text;
} catch (err) {
    this.buffer += data;   // ← [REWRITE] xxx 从这里进了界面
}
```

改造前，用户每次对话都能在回答开头看到字面的 `[REWRITE] 天津三日游攻略`。**服务端约定的前缀协议，前端只实现了一半，缺的那一半静默降级成"当作正文渲染"。**

### 根因

新增自定义 SSE 前缀时，服务端和前端没有做同步。判据是前缀字符串，前端靠 `startsWith` 逐个匹配，未匹配的一律走兜底分支——而那个兜底分支是"当作 token 正文"，正好把协议标记显示给了用户。

### 修复

前端补上 `[INTENT]` 和 `[REWRITE]` 两个分支，渲染成状态条。同时**对改写文本做 HTML 转义**：它是模型输出，直接拼进 `innerHTML` 等于开一个新的 XSS 注入点。意图标签走白名单映射，未知值也转义后再渲染。

---

## 附带修复三：改写文本裸拼进 SSE 可能截断帧

### 问题现象

原实现是裸拼接：

```python
yield f"data: [REWRITE] {rewritten}\n\n"
```

SSE 的 `data:` 行以换行为界。**模型输出里只要出现换行，就会把这一帧截成两条**——第二条没有前缀，前端会当作正文 token 渲染出来。

### 修复

改用 json 编码，与 token 的帧格式一致；意图值是固定枚举、不含换行，可以裸拼：

```python
yield f"data: [INTENT] {intent}\n\n"
# 改写结果来自模型，用 json 编码避免其中的换行破坏 SSE 帧
yield f"data: [REWRITE] {json.dumps(resolved, ensure_ascii=False)}\n\n"
```

---

## 经验总结

| 要点                         | 说明                                                                                                                            |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------------------- |
| **"最坏等于没改"是最好的安全属性**       | 兜底目标不是"尽量正确"，而是**失败时退回改造前的行为**。做到这一点，改造就不可能是净负收益，才敢上                                                                          |
| **多个意图共用一坨提示词是问题根源**       | 输出契约（700 字、必须出 PDF）写在唯一的 system prompt 里，所有请求就都被它约束。拆提示词是分流的必要条件，不是可选项                                                        |
| **第三方 provider 的结构化输出要实测** | langchain 的 `with_structured_output` 默认 method 是**按 provider 能力推断**的，在 DeepSeek 上推成了不被支持的 `json_schema` 并直接 400。默认值可用是假设，不是保证 |
| **要枚举约束就不能用 json_mode**    | json_mode 只保证"输出是合法 JSON"，不保证字段取值在 schema 内。实测模型返回 `greeting` 这种自由值。需要约束就得走 function calling，把 schema 作为工具定义下发                |
| **提示词里的名字必须可校验**           | 8 个工具名全对不上而长期无人发现，根因是**提示词和工具注册表之间没有任何校验**。一致性测试是这类漂移的唯一防线，而且必须配一条"防假绿"的测试，否则扫不到东西时会静默通过                                      |
| **协议新增前缀要两端同改**            | 自定义 SSE 前缀协议没有类型系统兜底，前端 `startsWith` 未匹配时会落到兜底分支。这次的兜底分支恰好是"当正文渲染"，于是协议标记直接漏给了用户                                              |
| **模型输出进 HTML 必须转义**        | 改写文本和意图标签都是模型产出，拼进 `innerHTML` 前要转义。同一个页面里正文没转义是既有选择，但**不能因此新增更多注入点**                                                         |
| **约束靠机制，不靠自觉**             | 方案三（纯提示词分流）被排除，理由就是它把"该不该走重流程"交给模型自觉。同一份报告里"工具名错误长期无人发现"和"提示词分流不可靠"其实是同一个病                                                    |
| **意图不可观测就只能反推**            | 分类写进 `AgentState` 之后，日志能直接打出意图；否则只能从模型调了什么工具去猜它"认为"用户想干嘛                                                                      |
| **分类质量单测测不到**              | 单测能覆盖的是兜底和裁剪这类确定性边界。**模型分得准不准，只能靠带标签的评估集**，本项目目前没有                                                                            |

---

## 可改进点（后续优化）

1. **建分类质量评估集。** 本次只用 8 条话术粗测（8/8 正确），这不是准确率。需要 30–50 条带标签的真实话术，含跨类边界样本（如"帮我看看天津天气然后规划一下行程"——既是单点查询又要规划），跑准确率与混淆矩阵。**当前分类上线后如果分错，只能靠人工发现。**

2. **`mcp` 包未安装，MCP 工具实际从未加载成功。** 启动日志里的 `ModuleNotFoundError: No module named 'mcp'`：`langchain-mcp-adapters` 装了，但它依赖的 `mcp` 包没有。`main.py` 的粗粒度 `try/except` 把整个 MCP 加载失败吞成一条 warning，应用照常启动、只用 10 个本地工具。后果是 `search_image` 运行期根本不存在，**`plan_trip` 实测确实没有调用它，生成的攻略不会有配图**。这是本次之前就存在的环境缺口，但它同时说明"加载失败静默降级"这个设计需要重新考虑——至少要让接口能把降级状态暴露出去。

3. **意图在循环内不可修正。** 分类发生在进图之前，进图之后不再改变。模型执行中发现"这个请求其实需要生成 PDF"时，没有任何机制能改判。方案二（图内分类节点）能覆盖这个场景，代价是改核心控制流。

4. **前端只做过 curl 层验证。** `/travel` 返回 200、新增代码在位、SSE 线上格式正确都已确认，但环境里起不了浏览器，**状态条的渲染效果没有真机验证**。意图提示那两行的位置、样式、与工具状态条混排是否好看，需要人工过一眼。
