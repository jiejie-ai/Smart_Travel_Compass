# AI 旅游搭子（TravelManus）项目作品集

> 一个从 0 到 1 的 **RAG + Agent** 落地项目：用户输入一个目的地，智能体自动完成"查天气 → 搜攻略 → 查知识库 → 找图片 → 生成 PDF 攻略"的全流程。
> 本文包含项目背景、系统架构、技术栈，以及从 **发现问题 → 定位问题 → 解决问题 → 沉淀总结** 的一整套实战思路复盘。

---

## 一、项目背景

### 1.1 要解决的问题

传统的旅游攻略生成依赖人工在多平台（小红书、马蜂窝、天气网、地图 App）之间来回切换，信息碎片化、耗时且容易遗漏关键信息。本项目希望做一个"一句话出攻略"的 AI 助手：

> 用户输入 "帮我做一份北京的攻略"，Agent 自动完成：
> 
> 1. 把模糊问题改写为适合检索的完整查询
> 2. 联网搜索最新攻略（小红书式玩法、热门景点）
> 3. 从本地 RAG 知识库检索专业内容
> 4. 调用高德地图查天气、地理编码
> 5. 搜索目的地图片
> 6. 生成一份包含图文、路线、预算的 **PDF 旅游攻略** 并上传到云端供下载

### 1.2 核心难点

| 难点          | 说明                                              |
| ----------- | ----------------------------------------------- |
| **多工具编排**   | 一次回答需要串联 10+ 个工具，顺序依赖（先 geocode 拿 adcode 才能查天气） |
| **多数据源融合**  | 联网搜索、本地知识库、地图数据、图片，如何让 LLM 综合出高质量答案             |
| **流式体验**    | 整个流程可能耗时 1-2 分钟，需要用 SSE 实时反馈每个工具的执行进度           |
| **可靠性与稳定性** | Agent 循环可能死循环、反复重启、中途崩溃，需要控制流层面的兜底              |

### 1.3 项目周期

- 2026-06-26 项目初始化（FastAPI + LangChain 骨架）
- 2026-06-27 知识库检索链路打通，解决 3 个 RAG 关键问题
- 2026-07-10 PDF 图片嵌入改造，解决 5 个连环问题（MCP 兼容 / 递归上限 / 死循环 / 提示词重启 / 图片语言偏差）
- 2026-07-10 检索前优化（查询改写 + 多查询扩展）

---

## 二、技术栈

| 层次       | 技术                                              | 用途                     |
| -------- | ----------------------------------------------- | ---------------------- |
| 语言       | Python 3.12+                                    | 全项目                    |
| Web 框架   | FastAPI + Uvicorn                               | 后端服务、SSE 流式接口          |
| Agent 编排 | LangChain + LangGraph                           | LLM 调用、状态机图编排、ToolNode |
| LLM      | DeepSeek（`deepseek-chat`，经 OpenAI 兼容接口）         | 查询改写、Agent 决策、回答生成     |
| RAG 知识库  | RAGFlow（Docker 部署，含 ES + MySQL + Redis + MinIO） | 本地专业知识库向量检索            |
| 联网搜索     | Tavily API                                      | 实时攻略、最新资讯              |
| 地图数据     | 高德地图 API（geocode / weather）                     | 地理编码、天气查询              |
| 图片搜索     | Pexels API（经 MCP Server）                        | 目的地图片                  |
| MCP 协议   | `mcp` / `langchain-mcp-adapters` / `FastMCP`    | 扩展工具（图片搜索、高德地图）        |
| PDF 生成   | ReportLab（含中文字体注册）                              | 生成图文攻略 PDF             |
| 对象存储     | 腾讯云 COS（`cos-python-sdk-v5`）                    | 上传 PDF 提供下载链接          |
| 前端       | Jinja2 + 原生 HTML/CSS/JS                         | Web 交互页面               |
| 配置       | pydantic-settings                               | `.env` 环境配置            |

---

## 三、系统架构

### 3.1 整体架构图

```
┌──────────────────────────┐
│       Web 前端页面         │
│  （旅行攻略对话页）          │
└────────────┬─────────────┘
             │ SSE 流式事件（on_chat_model_stream / on_tool_start / on_tool_end）
┌────────────▼─────────────┐
│       FastAPI 服务         │
│  ┌─────────────────────┐  │
│  │  查询改写（DeepSeek）   │  │  模糊问题 → 清晰检索语句
│  └──────────┬──────────┘  │
│  ┌──────────▼──────────┐  │
│  │  LangGraph Agent 图   │  │  check_state → thinker → actor → ... → finalize
│  └──────────┬──────────┘  │
└─────────────┼─────────────┘
      ┌───────┴────────┐
      ▼                ▼
 本地工具层           MCP 工具层
 ┌────────────────┐  ┌────────────────┐
 │ search_web      │  │ search_image    │ ← FastMCP + Pexels
 │ (Tavily)        │  │ (Pexels 图片)    │
 │ search_knowledge│  │ amap-maps       │ ← 高德 MCP
 │ _base (RAGFlow) │  └────────────────┘
 │ geocode/weather │
 │ (高德)           │
 │ scrape_web (bs4)│
 │ generate_pdf    │
 │ (ReportLab+COS) │
 │ file/download   │
 └────────────────┘
```

### 3.2 Agent 状态机（LangGraph）

核心编排逻辑在 `src/agent/graph.py`，是一个经典的四节点循环图：

```
START → check_state → thinker →(有工具调用)→ actor(ToolNode)
                          ↑                        │
                          └────(未结束)─────────────┘
                          │ 结束
                          ▼
                       finalize → END
```

- **check_state**：初始化 `agent_state=RUNNING`
- **thinker**：LLM 决策节点，绑定工具后调用，返回下一轮的工具调用指令
- **actor**：`ToolNode`，实际执行 thinker 指定的工具
- **finalize**：标记 `FINISHED` 并结束

循环由两个条件边控制：

- `_should_continue`（thinker → ?）：若 LLM 产生了 `tool_calls` 则去 actor，否则 finalize
- `_should_loop`（actor → ?）：若任务未结束则回 thinker，否则 finalize

### 3.3 数据流

```
用户输入 "北京"
   │
   ▼ 查询改写（一次 DeepSeek 调用）
改写后 "北京旅游攻略：必去景点推荐、特色美食、最佳旅游季节、交通出行建议"
   │  [REWRITE] 事件透传给前端
   ▼ build_initial_state()
Agent 循环执行：
   1. geocode("北京") → 拿到 adcode
   2. search_web + search_knowledge_base（内部做多查询扩展）
   3. get_weather(adcode)
   4. 针对性补充搜索（美食 / 交通 / 住宿）
   5. search_image("天津","Tianjin travel")
   6. generate_pdf(完整攻略 + 图片 URL)
   7. do_terminate()
   │
   ▼
SSE 实时输出 LLM token 与工具执行进度
最终回答（700 字完整攻略）+ PDF 云端下载链接
```

### 3.4 API 接口

| 接口                              | 方式  | 说明                                |
| ------------------------------- | --- | --------------------------------- |
| `GET /api/ai/health`            | —   | 健康检查                              |
| `GET /api/ai/travel/chat/sync`  | 同步  | 普通旅游对话（带文件记忆）                     |
| `GET /api/ai/travel/chat/sse`   | SSE | 流式对话                              |
| `GET /api/ai/travel/agent/chat` | SSE | **Agent 全流程**：改写 + 多工具编排 + PDF 生成 |

---

## 四、核心功能拆解

### 4.1 本地工具（`src/tools/`，10 个）

| 工具                         | 底层服务            | 职责                |
| -------------------------- | --------------- | ----------------- |
| `search_web`               | Tavily          | 联网搜索攻略            |
| `search_knowledge_base`    | RAGFlow         | 本地知识库检索（含多查询扩展）   |
| `geocode`                  | 高德              | 地名 → 经纬度 + adcode |
| `get_weather`              | 高德              | 实时天气 / 4 天预报      |
| `scrape_web`               | requests + bs4  | 抓取指定 URL 正文       |
| `generate_pdf`             | ReportLab + COS | 生成图文 PDF 并上传      |
| `read_file` / `write_file` | 本地文件            | 读写中间文件            |
| `download_resource`        | httpx           | 下载资源              |
| `do_terminate`             | —               | 结束 Agent 循环       |

### 4.2 MCP 工具

通过 `MultiServerMCPClient` 以 stdio 子进程方式接入：

| 服务                | 实现                     | 能力                 |
| ----------------- | ---------------------- | ------------------ |
| `yu-image-search` | 自研 `FastMCP`，封装 Pexels | 目的地图片搜索（含中英城市映射兜底） |
| `amap-maps`       | 高德官方 MCP Server        | 地图/POI 能力          |

### 4.3 RAG 检索前优化（`src/rag/` + `src/api/router.py`）

- **查询改写**：用户请求进入 Agent 前，先用 DeepSeek 把模糊问题改写为完整查询（复用同一模型，零额外成本）
- **多查询扩展**：`search_knowledge_base` 内部按 `["景点","美食","交通","住宿","天气","攻略","门票","文化"]` 8 个维度拆成 5 个子查询分别检索，去重合并，提升召回率

---

## 五、从"发现问题 → 解决问题 → 沉淀总结"的完整思路

这是本项目最核心的价值部分。改错日志里记录了 **8 个真实问题的完整复盘**，我在实战中逐步形成了一套可复用的排查方法论，并把它抽象成下面这个"六步循环"。

### 5.1 一套可复用的排查方法论

```
① 观察现象          → 把"失败"具体化：日志、响应 body、最终输出
② 分层定位          → 逐层排除：请求链路 / 检索层 / 生成层 / 提示词层 / 依赖层
③ 最小化复现        → 绕过复杂链路直接调 API，排除无关变量
④ 根因分析          → 深挖"为什么"，不满足于表面现象
⑤ 修复并验证        → 最小改动 + 端到端重新走一遍
⑥ 沉淀总结          → 经验表格化，提取可复用的设计模式
```

### 5.2 问题全景地图

| #   | 问题                 | 所属层     | 一句话根因                                 | 核心教训            |
| --- | ------------------ | ------- | ------------------------------------- | --------------- |
| 1   | RAGFlow 检索始终返回空    | 第三方 API | 参数 `kb_ids` 应为 `dataset_ids`，错误码被静默丢弃 | 不要只信任 HTTP 状态码  |
| 2   | 检索成功但 LLM 不采纳      | 生成层     | 50 字孤证被 LLM 判定为噪声                     | 检索 ≠ 生成，两层筛选    |
| 3   | 误以为文档没被检索          | 认知偏差    | 文档内容是路线而非景点清单                         | RAG 不是文件名搜索     |
| 4a  | MCP Server API 不兼容 | 依赖层     | `mcp` 库升级，`Server.tool` 改为 `FastMCP`  | 语义版本 ≠ 稳定性      |
| 4b  | Agent 中途崩溃         | 控制流     | `recursion_limit=25` 不够 9 步流程         | 引擎限制 vs 业务计数器   |
| 4c  | do_terminate 后死循环  | 控制流     | 条件边终止条件缺失                             | 工具副作用必须被检查      |
| 4d  | 反复重启工作流            | 提示词层    | 步骤列表从"参考"变成"命令"                       | 让 LLM 续继而非重启    |
| 4e  | Pexels 中文搜图不准      | 数据源     | 海外图库英文标签，中文语义覆盖差                      | 不要假设海外 API 支持中文 |

### 5.3 三个最有代表性的案例深度复盘

#### 案例 A：RAGFlow 检索始终返回空结果 —— "最小化复现"的价值

**问题现象**：用户提问后 Agent 调用知识库检索，6 次 HTTP 请求全部返回 200，但 `chunks` 永远为空数组。上传的测试文档"彩虹胡同"从未出现在回答中。

**排查过程（三步排除法）**：

1. 看服务端日志 → 确认工具确实被调用、请求确实发到了 RAGFlow → 排除"工具未调用"
2. 浏览器登录 RAGFlow 管理界面 → 确认知识库有数据、文档解析成功 → 排除"知识库为空"
3. **用 Python 脚本绕过整个 Agent 链路直接调 RAGFlow API** → 发现致命线索：

```python
# 传代码中的参数名 → 返回
{"code": 102, "message": "`dataset_ids` is required."}
# 换成正确参数名 → 数据正常返回
{"code": 0, "chunks": [{"content": "...彩虹胡同...门票88元..."}]}
```

**根因**：`src/tools/ragflow_search.py` 请求体把 `dataset_ids` 写成了 `kb_ids`。RAGFlow 返回业务错误码 `102`，但 HTTP 状态码是 200，代码只做了 `raise_for_status()` 就读取 `chunks`，拿到空数组直接返回"未找到"——**真正的错误信息被静默丢弃了**。

**修复**：1 行改动（`kb_ids` → `dataset_ids`）。

**方法论沉淀**：

- 很多 API（RAGFlow、部分云服务）业务失败时仍返回 HTTP 200，**真正的错误码在 response body 里**
- 善用"烟雾测试"：知识库放一条只有自己知道的假数据，能瞬间验证端到端链路
- 最小化复现：绕过 Agent/Graph/提示词等复杂链路，直接调 API，把变量减到最少
- 注意：Windows PowerShell 的 GBK 编码会让 curl 里的中文变乱码，用 Python 脚本发请求可避开编码陷阱

---

#### 案例 B：检索成功但 LLM 不采纳 —— "检索 ≠ 生成"

**问题现象**：修复 `dataset_ids` 后日志显示"检索成功，返回 7 条结果"，但最终回答和 PDF 里依然没有"彩虹胡同"。**数据取回来了，Agent 却不用。**

**根因**：RAG 系统有**两层筛选**：

| 层级  | 做什么                     | 本次状态     |
| --- | ----------------------- | -------- |
| 检索层 | 向量相似度匹配，返回 top-N chunks | 正常       |
| 生成层 | LLM 综合所有工具返回，做可信度判断     | 过滤了低质量数据 |

"彩虹胡同"只有 1 句话（约 50 字），而同批返回的 Citywalk 攻略每条 300-800 字、结构完整。LLM 在做信息综合时，把短小孤立的孤证判定为噪声丢弃。用 1200+ 字、结构完整（含表格/路线/价格/交通）的测试文档替换后，内容成功进入回答。

**修复（三层联动）**：

1. 提示词层：在 `TRAVEL_MANUS_NEXT_STEP_PROMPT` 增加知识库优先指令，明确"必须包含所有知识库条目，不得过滤"
2. 工具返回格式层：`search_knowledge_base` 返回值前追加 `[权威知识库来源]` 权威标识前缀
3. 数据质量层：烟雾测试文档必须与真实数据保持同等丰富度和格式

**方法论沉淀**：

- **检索拿到的数据 LLM 不一定采纳**，生成阶段自带可信度过滤——排查"内容没出现"时必须区分是检索层问题还是生成层问题
- 测试数据要逼真，一行假数据会被当噪声
- **Prompt Engineering 是杠杆**：在提示词中明确优先级，比改代码逻辑更直接有效

---

#### 案例 C：Agent 的三个"控制流"连环问题 —— 状态机设计的系统性思考

在 PDF 图片嵌入改造中，一次性暴露了 3 个相互关联的控制流问题，非常适合说明"系统性问题要系统性解决"：

**C1. LangGraph 递归上限不足**

```
Recursion limit of 25 reached without hitting a stop condition.
```

| 参数                | 默认值 | 含义                               |
| ----------------- | --- | -------------------------------- |
| `recursion_limit` | 25  | LangGraph 引擎执行节点的**物理硬上限**，到即抛异常 |
| `max_steps`       | 20  | AgentState 里的**业务计数器**，只被判断逻辑消费  |

Agent 图每轮循环 = thinker(1) + actor(1) = **2 次节点访问**，25 的上限只够跑约 12 轮，而完整 9 步流程需要更多。修复：`astream_events(..., config={"recursion_limit": 100})`。

**C2. do_terminate 后死循环**

Agent 完成工作后，反复执行"检查进度 → do_terminate → 检查进度 → do_terminate..."10+ 次。根因是**条件边终止条件缺失**：

- `do_terminate` 只是个普通工具，返回字符串"任务结束"，被包成 ToolMessage 塞进消息列表
- 它**没有能力修改 AgentState 的 `agent_state` 字段**（改状态只能靠节点函数 `_finalize`）
- `_should_loop` 又只检查 `agent_state` 和 `current_step`，两者都没变 → 无条件返回 "thinker"
- LLM 看到进度全 ✅ → 再次调用 do_terminate → **恒真回路**

修复：`_should_loop` 里检测最后一条消息内容是否包含"任务结束"。

**C3. Agent 不断自我怀疑、反复重启工作流**

完成一轮工作后不进入 PDF 生成，而是调用 `read_file` 读自己生成的内容，然后说"重新完整执行所有步骤"。根因在提示词：`TRAVEL_MANUS_NEXT_STEP_PROMPT` 每一轮都注入**完整 9 步列表 + CRITICAL/MUST 强措辞**，LLM 每次看到就认为自己"还没开始"。

修复：把"步骤列表"改为"进度检查清单"：

```
✅ Weather info gathered?
✅ Attractions info gathered?
...
If ALL done, call doTerminate immediately.
```

**三个问题的系统性关联**：

```
recursion_limit 不够  →  Agent 物理上跑不完 9 步
terminate 条件缺失    →  Agent 逻辑上停不下来
提示词触发重启        →  Agent 情感上不愿意结束
```

三者分别对应 **执行上限 / 终止条件 / 决策引导** 三个层面，共同构成了"让一个多工具 Agent 稳定跑完并正确结束"的完整问题域。这也是我在这个项目里最重要的认知升级：**Agent 开发的核心难点不是调用 API，而是控制流与提示词工程的协同设计。**

### 5.4 数据源语言的"三层兜底"设计

Pexels 图片搜索"天津"返回哈尔滨、上海的照片——海外图库以英文标签为主，中文语义覆盖严重不足。这是**上游数据源语言偏向**问题，无法靠调参解决。

方案选型权衡：

| 方案                 | 优点         | 缺点               |
| ------------------ | ---------- | ---------------- |
| A. 工具内嵌中英城市映射表     | 可靠，不依赖 LLM | 需要维护映射表          |
| B. LLM 提供 query_en | 灵活，无需维护    | 依赖 LLM 的"记性"，不可靠 |
| C. 换百度图片 API       | 中文支持最好     | 需要额外申请开发者账号      |

最终选择 **A + B 双重保险**，形成三层兜底：

```python
# 优先级：query_en > 城市映射表 > 中文+China 后缀 > 原始 query
if query_en:                    search_query = query_en        # 第1层：LLM 主动传
elif mapped:                    search_query = mapped          # 第2层：内置26城映射表
elif 含中文字符:                 search_query = f"{query} China" # 第3层：中文兜底
else:                           search_query = query           # 兜底
```

方法论沉淀：

- **功能可用 ≠ 质量达标**：Pexels 支持中文搜索"有结果"，不代表结果准确
- **不要依赖 LLM 的记性**：即使提示词写了明确指令，LLM 也可能忽略，关键逻辑应写在代码层
- **三层兜底架构**：用户输入 → LLM 处理层 → 代码逻辑层 → 兜底层，每层解决上一层的遗漏

### 5.5 RAG 检索前优化（主动提升质量）

在解决"检索结果不理想"的过程中，我进一步实现了**检索前优化（Pre-Retrieval Optimization）**，在查询阶段就提升输入质量，比检索后重排序更轻量、延迟更低。

**策略一：多查询扩展（Query Expansion）**

纯字符串拼接 + 固定维度列表，零延迟。解决"短关键词向量检索召回不全"：

```
"北京" → ["北京", "北京 景点", "北京 美食", "北京 交通", "北京 住宿", ...]
      → RAGFlow 多次检索 → 去重合并 → 截断 3000 字
```

**策略二：查询改写（Query Rewriting）**

用 DeepSeek 把模糊问题改写为清晰检索语句，优化的是"用户问什么"：

| 用户输入       | 改写后                                |
| ---------- | ---------------------------------- |
| "北京"       | "北京旅游攻略：必去景点推荐、特色美食、最佳旅游季节、交通出行建议" |
| "成都有什么好吃的" | "成都美食推荐：本地人必吃的特色小吃、火锅店、川菜馆和美食街"    |

**为何分层设计**：

1. 改写在前、扩展在后：改写优化"用户问什么"，扩展优化"知识库怎么查"，是不同层次的问题
2. 扩展只用于知识库：Tavily 搜索引擎本身对长查询理解更好；RAGFlow 向量检索对短查询敏感，适合扩展
3. 改写依赖 LLM 但扩展不依赖：改写必须语义理解，扩展是纯字符串操作

### 5.6 面试向总结：如何提升 RAG 召回率

在检索前优化的基础上，我整理了一套完整的召回率优化思路（按性价比排序）：

1. **分块策略调优**（影响最直接）：中文文档按段落分块 + 200~500 token + 10%~20% 重叠，防止"故宫门票旺季60元"这类跨边界信息被切断
2. **Embedding 模型选型**：中文场景换 `bge-large-zh-v1.5` / `m3e-large` 等中文语义模型，通常可提升 10~20% 召回
3. **开启混合检索**：纯向量检索对精确关键词可能漏召回，BM25 关键词 + 向量语义互补
4. **引用重排序（Reranker）**：接入 `bge-reranker-large` 对粗召回结果二次精排，提升 top-N 命中率
5. **保证文档解析质量**：扫描件/表格混排会拖累后续全链路，上传前先清洗重排版
6. **查询端增强（代码层已实现）**：Query Rewrite + 多查询扩展，不碰知识库配置就能见效

优先级建议：**先检查分块和 Embedding → 开启混合检索 → 加 Reranker**，三步改动最小、收益最大。

---

## 六、项目价值与能力沉淀

### 6.1 技术能力体现

- **Agent 编排**：LangGraph 状态机设计、条件边路由、ToolNode 执行、SSE 流式事件透传
- **RAG 工程**：RAGFlow 接入、检索前优化（改写 + 扩展）、知识库质量治理、召回率方法论
- **工具链集成**：本地工具 + MCP 协议扩展（FastMCP 自研 Server + 第三方 MCP）
- **后端工程**：FastAPI 分层架构、流式接口、Docker 部署 RAGFlow、云存储集成
- **调试能力**：分层定位、最小化复现、烟雾测试、API 错误码深挖

### 6.2 排查思维沉淀（可复用到任何项目）

1. **把失败具体化**：先说清楚"什么没发生 / 什么多做了"，再动手
2. **一层一层隔离**：请求链路 → 检索层 → 生成层 → 提示词层 → 依赖层，每层用一个最小实验排除
3. **不要只信任 HTTP 状态码**：业务错误常藏在 200 的 body 里
4. **测试数据要逼真**：假数据要能代表真实使用场景，否则会误判
5. **工具副作用必须被检查**：调用方的意图（结束）与决策方（路由）分离时最容易遗漏
6. **提示词是杠杆也是坑**：MUST/CRITICAL 强措辞会让 LLM 过度执行；进度清单比步骤列表更适合循环场景
7. **关键逻辑写在代码层**：不要依赖 LLM 的"记性"遵守约定

### 6.3 一句话总结

> 这个项目让我从"能调 API"进阶到"能设计一个稳定跑完的多工具 Agent"：**RAG 解决"知识从哪来"，Agent 解决"工作怎么做完"，而把它们粘合在一起的，是一套可复用的定位与沉淀方法论。**
