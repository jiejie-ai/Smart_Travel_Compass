# AI 旅游搭子 启动指南

## 环境要求

- Python >= 3.12
- 已安装依赖（`.venv` 目录存在）

激活ragflow，先启动虚拟机SpringAI，然后cd ~/ragflow/docker,然后docker compose up -d，最后docker ps查看是否运行，正常运行如下：

yuanzijie@yuanzijie-virtual-machine:~~/ragflow/docker$ docker compose up -d
[+] up 5/5
 ✔ Container docker-es01-1 Running 0.0s
 ✔ Container docker-mysql-1 Healthy 0.6s
 ✔ Container docker-ragflow-cpu-1 Running 0.0s
 ✔ Container docker-minio-1 Running 0.0s
 ✔ Container docker-redis-1 Running 0.0s
yuanzijie@yuanzijie-virtual-machine:~~/ragflow/docker$ docker ps
CONTAINER ID IMAGE COMMAND CREATED STATUS PORTS NAMES
8fc7d3998947 docker.1ms.run/infiniflow/ragflow:latest "./entrypoint.sh --e…" 2 weeks ago Up 9 minutes 0.0.0.0:443->443/tcp, [::]:443->443/tcp, 0.0.0.0:9380-9384->9380-9384/tcp, [::]:9380-9384->9380-9384/tcp, 0.0.0.0:81->80/tcp, [::]:81->80/tcp docker-ragflow-cpu-1
dfc0c404b1da elasticsearch:8.11.3 "/bin/tini -- /usr/l…" 2 weeks ago Up 9 minutes (healthy) 9300/tcp, 0.0.0.0:1200->9200/tcp, [::]:1200->9200/tcp docker-es01-1
e697ff103457 mysql:8.0.39 "docker-entrypoint.s…" 2 weeks ago Up 9 minutes (healthy) 0.0.0.0:3306->3306/tcp, [::]:3306->3306/tcp, 33060/tcp docker-mysql-1
731682a94162 valkey/valkey:8 "docker-entrypoint.s…" 2 weeks ago Up 9 minutes (healthy) 0.0.0.0:6379->6379/tcp, [::]:6379->6379/tcp docker-redis-1
d2d08e0f49f1 pgsty/minio:RELEASE.2026-03-25T00-00-00Z "/usr/bin/docker-ent…" 2 weeks ago Up 9 minutes (healthy) 0.0.0.0:9000-9001->9000-9001/tcp, [::]:9000-9001->9000-9001/tcp docker-minio-1

输入192.168.138.137:81即可在浏览器里访问
(启动后可能会报502错误，请耐心等待几分钟就行)
## 快速启动

### 1. 激活虚拟环境

```powershell
.venv\Scripts\activate
```

### 2. 启动服务

```powershell
uvicorn src.main:app --reload --host 0.0.0.0 --port 8123
```

### 3. 访问

| 地址                         | 说明             |
| -------------------------- | -------------- |
| http://localhost:8123      | Web 页面         |
| http://localhost:8123/docs | Swagger API 文档 |

---

## 其他启动方式

### 使用 uv（需要先安装 uv）

```powershell
powershell -c "irm https://astral.sh/uv/install.ps1 | iex"
# 重启终端后
uv run uvicorn src.main:app --reload --port 8123
```

### Docker 启动

```powershell
docker build -t ai-tourism .
docker run -p 8123:8123 --env-file .env ai-tourism
```

---

## 配置说明

配置文件为项目根目录的 `.env`，缺失时复制 `.env.example` 并填写：

| 变量                             | 说明                     |
| ------------------------------ | ---------------------- |
| OPENAI_API_KEY                 | LLM API Key（必填）        |
| OPENAI_BASE_URL                | LLM API 地址，默认 DeepSeek |
| CHAT_MODEL                     | 模型名，默认 deepseek-chat   |
| AMAP_API_KEY                   | 高德地图 API Key           |
| TAVILY_API_KEY                 | Tavily 搜索 API Key      |
| PEXELS_API_KEY                 | Pexels 图片搜索 API Key    |
| RAGFLOW_BASE_URL               | RAGFlow 知识库地址          |
| RAGFLOW_API_KEY                | RAGFlow API Key        |
| RAGFLOW_KB_ID                  | RAGFlow 知识库 ID         |
| COS_SECRET_ID / COS_SECRET_KEY | 腾讯云 COS 对象存储           |
| COS_REGION / COS_BUCKET_NAME   | COS 区域和桶名              |
| SERVER_PORT                    | 服务端口，默认 8123           |

---

## 首次安装依赖

```powershell
# 创建虚拟环境
python -m venv .venv
.venv\Scripts\activate

# 安装
pip install -e .
```
